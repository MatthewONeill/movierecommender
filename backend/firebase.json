{
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run lint"
    ]
  },

  "headers": [{
    "source" : "bundle.js",
    "headers" : [{
      "key" : "Access-Control-Allow-Origin",
      "value" : "*"
    }]
  }]
}
